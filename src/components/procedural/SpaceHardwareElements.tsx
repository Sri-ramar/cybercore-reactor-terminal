import React, { memo } from 'react';
import { ThemeConfig } from '../../types';

interface SpaceHardwareElementsProps {
  theme: ThemeConfig;
}

/**
 * SpaceHardwareElements:
 * 3D satellite and photovoltaic arrays are natively rendered in the photorealistic base layer.
 */
export const SpaceHardwareElements: React.FC<SpaceHardwareElementsProps> = memo(() => {
  return null;
});
